{
  "name": "spamblock/spamblock",
  "description": "Spamblock osTicket plugin",
  "type": "project",
  "license": "proprietary",
  "require": {
    "php": ">=8.2"
  },
  "require-dev": {
    "phpunit/phpunit": "^11.0"
  },
  "autoload-dev": {
    "classmap": [
      "tests/"
    ]
  },
  "scripts": {
    "test": "phpunit --configuration phpunit.xml",
    "test:integration": "phpunit --configuration phpunit.integration.xml --group integration"
  }
}
